import Topics from "../components/Topics"
import { fetchArticlesByTopic } from "./nc-news-API"

// fetch topic
// set results in state


// export const sortArticlesByTopicSlug = (slug) => {
//     fetchArticlesByTopic(slug).then((articles) => {
//         console.log(articles);
//         return articles;
//     })
// }